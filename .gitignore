# Build artifacts — never ship these.
# Unanchored patterns so they match at any depth, and survive folder renames.
obj/
bin/
*.user
*.suo
.vs/

# macOS
.DS_Store

# NOTE: the compiled assembly IS committed deliberately.
# Steam Workshop subscribers and GitHub-release users cannot build from source,
# so "Dried Fish/Assemblies/DriedFish.dll" must stay in the repo.
# Do not add Assemblies/ or *.dll to this file.
